import type { DynamicClient } from '../../../client/types';
/**
 * Maps the state keys to the event names.
 *
 * This is mainly to ensure that whenever a new state key is added, the developer
 * will have to come to this file and remember to add it here and in the interface.
 */
export declare const stateChangeEvents: {
    initStatus: "changed:initStatus";
    projectSettings: "changed:projectSettings";
    token: "changed:token";
    user: "changed:user";
};
declare global {
    interface DynamicEvents {
        'changed:initStatus': (value: DynamicClient['initStatus'], previous: DynamicClient['initStatus']) => void;
        'changed:projectSettings': (value: DynamicClient['projectSettings'], previous: DynamicClient['projectSettings']) => void;
        'changed:token': (value: DynamicClient['token'], previous: DynamicClient['token']) => void;
        'changed:user': (value: DynamicClient['user'], previous: DynamicClient['user']) => void;
    }
}
//# sourceMappingURL=events.d.ts.map