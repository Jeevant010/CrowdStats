export type StoreAuthTokenProps = {
    jwt?: string;
    minifiedJwt?: string;
    expiresAt: number;
};
export declare const storeAuthToken: ({ jwt, minifiedJwt, expiresAt, }: StoreAuthTokenProps) => void;
