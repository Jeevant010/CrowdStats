import { Wallet } from '@dynamic-labs/wallet-connector-core';
export type FundWithWalletParams = {
    amount: number;
    fromWallet: Wallet;
    token?: {
        address: string;
        decimals?: number;
    };
};
export declare const useFund: () => ({ amount, fromWallet, token }: FundWithWalletParams) => Promise<string | undefined>;
