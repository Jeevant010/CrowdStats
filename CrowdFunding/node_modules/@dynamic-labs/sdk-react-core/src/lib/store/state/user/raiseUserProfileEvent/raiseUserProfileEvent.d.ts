import { UserProfile } from '@dynamic-labs/types';
type UserProfileEventPayload = {
    oldUser: UserProfile;
    newUser: UserProfile;
};
export declare const hasFieldChanged: <T extends string>(fields: T[], record1: Partial<Record<T, unknown>> | undefined, record2: Partial<Record<T, unknown>> | undefined) => boolean;
export declare const raiseUserProfileEvent: ({ oldUser, newUser, }: UserProfileEventPayload) => void;
export {};
