import { Wallet } from '../../../../../../shared/types/wallets';
import { FundingTokenData } from '../../types';
type UseTokensForFundingProps = {
    wallet: Wallet;
};
export declare const useTokensForFunding: ({ wallet, }: UseTokensForFundingProps) => {
    isLoading: boolean;
    tokens: FundingTokenData[];
};
export {};
